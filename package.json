{
  "name": "rift-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "bun run generate:config",
    "generate:config": "node scripts/generate-config-reference.mjs",
    "check": "bun run generate && node scripts/check-docs.mjs",
    "dev": "bun run generate && ASTRO_TELEMETRY_DISABLED=1 astro dev",
    "build": "bun run generate && ASTRO_TELEMETRY_DISABLED=1 astro build",
    "preview": "bun run generate && ASTRO_TELEMETRY_DISABLED=1 astro preview",
    "check:rust": "bun run generate && node scripts/check-config-rust.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.0.2",
    "starlight-theme-next": "^0.4.0"
  },
  "devDependencies": {
    "smol-toml": "^1.8.0"
  }
}
